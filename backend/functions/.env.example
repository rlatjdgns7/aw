# Firebase Functions Environment Variables Example
# Note: Firebase Functions automatically uses default service account credentials
# Do not set GOOGLE_APPLICATION_CREDENTIALS in production

# Optional: Firebase Project Configuration  
FIREBASE_PROJECT_ID=foodism-782cb

# Development Environment Variables
NODE_ENV=development

# Important Notes:
# - PORT is reserved and managed automatically by Firebase Functions
# - GOOGLE_APPLICATION_CREDENTIALS should not be set for Firebase Functions
# - Local development can use Firebase emulator without service account keys